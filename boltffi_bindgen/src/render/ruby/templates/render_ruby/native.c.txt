// Generated by BoltFFI — do not edit.
#include "ruby.h"
#include "ruby/encoding.h"
{% if ractor_safe %}#include "ruby/ractor.h"
{% endif %}#include <stdint.h>
#include <limits.h>
#include <string.h>

typedef struct { const uint8_t *ptr; uintptr_t len; } FfiString;
typedef struct { const uint8_t *ptr; uintptr_t len; } FfiBytes;
typedef struct { uint8_t *ptr; uintptr_t len; uintptr_t cap; uintptr_t align; } FfiBuf;
extern void boltffi_free_buf(FfiBuf);

static inline FfiString bolt_rb_to_ffi_string(VALUE v) {
    FfiString s; s.ptr = (const uint8_t*)RSTRING_PTR(v); s.len = (uintptr_t)RSTRING_LEN(v); return s;
}
static inline FfiBytes bolt_rb_to_ffi_bytes(VALUE v) {
    FfiBytes b; b.ptr = (const uint8_t*)RSTRING_PTR(v); b.len = (uintptr_t)RSTRING_LEN(v); return b;
}

{% for helper in array_helpers %}typedef struct { const {{ helper.c_elem_type }} *ptr; uintptr_t len; } {{ helper.vec_c_type }};
static VALUE marshal_{{ helper.name }}_array({{ helper.vec_c_type }} v) {
    VALUE ary = rb_ary_new_capa((long)v.len);
    VALUE buf[128];
    size_t i = 0;
    for (size_t j = 0; j < v.len; j++) {
        buf[i++] = {{ helper.c_to_rb }}(v.ptr[j]);
        if (i == 128) { rb_ary_cat(ary, buf, 128); i = 0; }
    }
    if (i > 0) rb_ary_cat(ary, buf, (long)i);
    return ary;
}
{% endfor %}
{% for class in classes %}typedef struct {{ class.c_struct_name }} {{ class.c_struct_name }};
{% endfor %}
{% for class in classes %}static VALUE c_{{ class.type_ident }};
static const rb_data_type_t {{ class.type_ident }}_type;
{% endfor %}
{% for class in classes %}static void {{ class.type_ident }}_dfree(void *p) {
    if (p != NULL) {
        extern void {{ class.ffi_free }}({{ class.c_struct_name }}*);
        {{ class.ffi_free }}(({{ class.c_struct_name }}*)p);
    }
}
{% endfor %}
{% for class in classes %}static const rb_data_type_t {{ class.type_ident }}_type = {
    "{{ class.qualified_name }}",
    { NULL, {{ class.type_ident }}_dfree, NULL, NULL },
    NULL, NULL, RUBY_TYPED_FREE_IMMEDIATELY,
};
{% endfor %}

{% for func in functions %}extern {{ func.c_return_type }} {{ func.sym }}({% if func.params.is_empty() %}void{% else %}{% for p in func.params %}{{ p.c_type }}{% if !loop.last %}, {% endif %}{% endfor %}{% endif %});
{% endfor %}
{% for class in classes %}{% for ctor in class.constructors %}extern {{ class.c_struct_name }}* {{ ctor.ffi_sym }}({% if ctor.params.is_empty() %}void{% else %}{% for p in ctor.params %}{{ p.c_type }}{% if !loop.last %}, {% endif %}{% endfor %}{% endif %});
{% endfor %}{% endfor %}
{% for class in classes %}{% for method in class.methods %}{% if method.is_static %}extern {{ method.c_return_type }} {{ method.ffi_sym }}({% if method.params.is_empty() %}void{% else %}{% for p in method.params %}{{ p.c_type }}{% if !loop.last %}, {% endif %}{% endfor %}{% endif %});
{% else %}extern {{ method.c_return_type }} {{ method.ffi_sym }}({{ class.c_struct_name }}*{% for p in method.params %}, {{ p.c_type }}{% endfor %});
{% endif %}{% endfor %}{% endfor %}
{% for class in classes %}
static VALUE {{ class.type_ident }}_alloc(VALUE klass) {
    return TypedData_Wrap_Struct(klass, &{{ class.type_ident }}_type, NULL);
}

{% for ctor in class.constructors %}{% if ctor.ruby_name == "new" %}static VALUE {{ class.type_ident }}_initialize(VALUE self{% for p in ctor.params %}, VALUE {{ p.ruby_name }}{% endfor %}) {
{% for p in ctor.params %}{% if p.needs_type_check %}    Check_Type({{ p.ruby_name }}, {{ p.ruby_type_tag }});
{% endif %}{% endfor %}    {{ class.c_struct_name }}* ptr = {{ ctor.ffi_sym }}({% for p in ctor.params %}{{ p.rb_to_c }}({{ p.ruby_name }}){% if !loop.last %}, {% endif %}{% endfor %});
{% if ctor.is_fallible %}    if (ptr == NULL) {
        extern void boltffi_last_error_message(FfiString* out);
        FfiString msg;
        boltffi_last_error_message(&msg);
        rb_raise(rb_eRuntimeError, "%.*s", (int)msg.len, (const char*)msg.ptr);
    }
{% endif %}    RTYPEDDATA_DATA(self) = ptr;
    return self;
}
{% else %}// Named constructor — defined as a singleton (class) method that allocates and
// wraps the Rust handle directly, rather than the alloc+initialize pair `new` uses.
static VALUE {{ class.type_ident }}_ctor_{{ ctor.ruby_name }}(VALUE klass{% for p in ctor.params %}, VALUE {{ p.ruby_name }}{% endfor %}) {
{% for p in ctor.params %}{% if p.needs_type_check %}    Check_Type({{ p.ruby_name }}, {{ p.ruby_type_tag }});
{% endif %}{% endfor %}    {{ class.c_struct_name }}* ptr = {{ ctor.ffi_sym }}({% for p in ctor.params %}{{ p.rb_to_c }}({{ p.ruby_name }}){% if !loop.last %}, {% endif %}{% endfor %});
{% if ctor.is_fallible %}    if (ptr == NULL) {
        extern void boltffi_last_error_message(FfiString* out);
        FfiString msg;
        boltffi_last_error_message(&msg);
        rb_raise(rb_eRuntimeError, "%.*s", (int)msg.len, (const char*)msg.ptr);
    }
{% endif %}    return TypedData_Wrap_Struct(klass, &{{ class.type_ident }}_type, ptr);
}
{% endif %}{% endfor %}
{% for method in class.methods %}static VALUE {{ class.type_ident }}_{{ method.ruby_name }}(VALUE self{% for p in method.params %}, VALUE {{ p.ruby_name }}{% endfor %}) {
{% if method.is_static %}    (void)self; // static method: `self` is the class object, unused
{% else %}    {{ class.c_struct_name }}* handle = rb_check_typeddata(self, &{{ class.type_ident }}_type);
{% if class.has_consuming_method %}    // A consuming method on this class nulls the handle; guard every method so a
    // call after consumption raises instead of dereferencing a freed handle.
    if (handle == NULL) rb_raise(rb_eRuntimeError, "use of consumed {{ class.class_name }}");
{% endif %}{% endif %}{% for p in method.params %}{% if p.needs_type_check %}    Check_Type({{ p.ruby_name }}, {{ p.ruby_type_tag }});
{% endif %}{% endfor %}{% if method.returns_void %}    {{ method.ffi_sym }}({{ method.ffi_call_args }});
{% if method.consumes_self %}    RTYPEDDATA_DATA(self) = NULL;{% endif %}
    return Qnil;
{% endif %}{% if method.returns_scalar %}    {{ method.scalar_c_type }} _ffi_ret = {{ method.ffi_sym }}({{ method.ffi_call_args }});
{% if method.consumes_self %}    RTYPEDDATA_DATA(self) = NULL;{% endif %}
    return {{ method.scalar_c_to_rb }};
{% endif %}{% if method.returns_handle %}    {{ class.c_struct_name }}* _ffi_ret = {{ method.ffi_sym }}({{ method.ffi_call_args }});
{% if method.consumes_self %}    RTYPEDDATA_DATA(self) = NULL;{% endif %}{% if method.is_fallible %}    if (_ffi_ret == NULL) {
        extern void boltffi_last_error_message(FfiString* out);
        FfiString msg;
        boltffi_last_error_message(&msg);
        rb_raise(rb_eRuntimeError, "%.*s", (int)msg.len, (const char*)msg.ptr);
    }
{% endif %}    return TypedData_Wrap_Struct(c_{{ method.returns_handle_type_ident }}, &{{ method.returns_handle_type_ident }}_type, _ffi_ret);
{% endif %}{% if method.returns_utf8_string %}    FfiBuf _ffi_ret = {{ method.ffi_sym }}({{ method.ffi_call_args }});
{% if method.consumes_self %}    RTYPEDDATA_DATA(self) = NULL;{% endif %}    { uint32_t _wire_len = 0;
      memcpy(&_wire_len, _ffi_ret.ptr, sizeof(uint32_t));
      VALUE _rb_ret = rb_utf8_str_new((const char*)(_ffi_ret.ptr + sizeof(uint32_t)), (long)_wire_len);
      RB_ENC_CODERANGE_SET(_rb_ret, ENC_CODERANGE_VALID);
      boltffi_free_buf(_ffi_ret);
      return _rb_ret; }
{% endif %}{% if method.returns_bytes %}    FfiBuf _ffi_ret = {{ method.ffi_sym }}({{ method.ffi_call_args }});
{% if method.consumes_self %}    RTYPEDDATA_DATA(self) = NULL;{% endif %}    // Vec<u8> returns a raw (non-wire-encoded) FfiBuf: ptr/len describe the bytes
    // directly, with no 4-byte length prefix (unlike String's wire_encode buffer).
    { VALUE _rb_ret = rb_str_new((const char*)_ffi_ret.ptr, (long)_ffi_ret.len);
      boltffi_free_buf(_ffi_ret);
      return _rb_ret; }
{% endif %}
}
{% endfor %}
{% endfor %}

{% for func in functions %}static VALUE rb_{{ func.ruby_name }}(VALUE self{% for p in func.params %}, VALUE {{ p.ruby_name }}{% endfor %}) {
{% for p in func.params %}{% if p.needs_type_check %}    Check_Type({{ p.ruby_name }}, {{ p.ruby_type_tag }});
{% endif %}{% endfor %}{% if func.returns_void %}    {{ func.sym }}({% for p in func.params %}{{ p.rb_to_c }}({{ p.ruby_name }}){% if !loop.last %}, {% endif %}{% endfor %});
    return Qnil;
{% else %}    {{ func.c_return_type }} _ffi_ret = {{ func.sym }}({% for p in func.params %}{{ p.rb_to_c }}({{ p.ruby_name }}){% if !loop.last %}, {% endif %}{% endfor %});
{% if func.returns_utf8_string %}    { uint32_t _wire_len = 0;
      memcpy(&_wire_len, _ffi_ret.ptr, sizeof(uint32_t));
      VALUE _rb_ret = rb_utf8_str_new((const char*)(_ffi_ret.ptr + sizeof(uint32_t)), (long)_wire_len);
      RB_ENC_CODERANGE_SET(_rb_ret, ENC_CODERANGE_VALID);
      boltffi_free_buf(_ffi_ret);
      return _rb_ret; }
{% endif %}{% if func.returns_bytes %}    // Vec<u8> returns a raw (non-wire-encoded) FfiBuf: ptr/len describe the bytes
    // directly, with no 4-byte length prefix (unlike String's wire_encode buffer).
    { VALUE _rb_ret = rb_str_new((const char*)_ffi_ret.ptr, (long)_ffi_ret.len);
      boltffi_free_buf(_ffi_ret);
      return _rb_ret; }
{% endif %}{% if !func.returns_utf8_string && !func.returns_bytes %}    return {{ func.c_to_rb }};
{% endif %}{% endif %}
}
{% endfor %}
void Init_{{ crate_name }}_native(void) {
{% if ractor_safe %}    // Declare every method defined below safe to call from any Ractor.
    // Opt-in (`[targets.ruby] ractor_safe`): asserts the bound Rust crate has no
    // unsynchronized global mutable state. Must precede the rb_define_* calls.
    rb_ext_ractor_safe(true);
{% endif %}    VALUE mod = rb_define_module("{{ module_name }}");
    VALUE native = rb_define_module_under(mod, "Native");

{% for class in classes %}    c_{{ class.type_ident }} = rb_define_class_under(mod, "{{ class.class_name }}", rb_cObject);
    rb_define_alloc_func(c_{{ class.type_ident }}, {{ class.type_ident }}_alloc);
{% for ctor in class.constructors %}{% if ctor.ruby_name == "new" %}    rb_define_method(c_{{ class.type_ident }}, "initialize", {{ class.type_ident }}_initialize, {{ ctor.param_count }});
{% else %}    rb_define_singleton_method(c_{{ class.type_ident }}, "{{ ctor.ruby_name }}", {{ class.type_ident }}_ctor_{{ ctor.ruby_name }}, {{ ctor.param_count }});
{% endif %}{% endfor %}{% for method in class.methods %}{% if method.is_static %}    rb_define_singleton_method(c_{{ class.type_ident }}, "{{ method.ruby_name }}", {{ class.type_ident }}_{{ method.ruby_name }}, {{ method.param_count }});
{% else %}    rb_define_method(c_{{ class.type_ident }}, "{{ method.ruby_name }}", {{ class.type_ident }}_{{ method.ruby_name }}, {{ method.param_count }});
{% endif %}{% endfor %}
{% endfor %}
{% for func in functions %}    rb_define_module_function(native, "{{ func.ruby_name }}", rb_{{ func.ruby_name }}, {{ func.param_count }});
{% endfor %}
}
